import React from 'react';

const style = {
  margin: "0"
}

const ContactJumbotron = ({children}) => <div className="jumbotron jumbotron-fluid" style={style}>{children}</div>

export default ContactJumbotron;